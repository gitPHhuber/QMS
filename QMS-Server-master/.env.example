PORT=5001
DB_HOST=127.0.0.1
DB_PORT=5434
DB_NAME=asvo_qms
DB_USER=qms
DB_PASSWORD=

# Keycloak
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=QMS-Realm
AUTH_MODE=dev-bypass
# AUTH_MODE=keycloak  # для продакшена
DEV_USER=developer
DEV_ROLE=SUPER_ADMIN
DEFAULT_ROLE=VIEWER
SECRET_KEY=

# ═══ Module System ═══
# Options: start | standard | pro | industry
# If not set — ALL modules enabled (dev mode)
# MODULES_TIER=pro

# OR individually (overrides MODULES_TIER):
# MODULES_ENABLED=qms.dms,qms.nc,qms.capa,wms.warehouse
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ═══ License System ═══
# License file path (default: config/license.lic)
# LICENSE_FILE=config/license.lic
# OR inline license key (overrides file):
# LICENSE_KEY=eyJleHAi...
# Ed25519 public key (base64). If not set, reads from config/als-public.key
# ALS_PUBLIC_KEY=

# Rate Limiting
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX=1000
