# Server
PORT=5001

# Database
DB_HOST=127.0.0.1
DB_PORT=5434
DB_NAME=asvo_qms
DB_USER=qms
DB_PASSWORD=qms_dev_2026
DB_SYNC=false          # true = sync tables on startup (alter mode)

# Auth
AUTH_MODE=dev-bypass    # keycloak | dev-bypass
DEV_USER=developer
DEV_ROLE=SUPER_ADMIN
DEFAULT_ROLE=VIEWER

# Keycloak (only if AUTH_MODE=keycloak)
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=QMS-Realm
